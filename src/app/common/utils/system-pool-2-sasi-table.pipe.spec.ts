import { SystemPool2SasiTablePipe } from './system-pool-2-sasi-table.pipe';

describe('SystemPool2SasiTablePipe', () => {
  it('create an instance', () => {
    const pipe = new SystemPool2SasiTablePipe();
    expect(pipe).toBeTruthy();
  });
  it('transform', () => {
    const pipe = new SystemPool2SasiTablePipe();
  });
});
