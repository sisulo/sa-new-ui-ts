import {StorageConvertPipe} from './storage-convert.pipe';

describe('StorageConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new StorageConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
