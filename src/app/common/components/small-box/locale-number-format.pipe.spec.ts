import { LocaleNumberFormatPipe } from './locale-number-format.pipe';

describe('LocaleNumberFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LocaleNumberFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
